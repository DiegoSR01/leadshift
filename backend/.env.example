# ── Database (Supabase) ──
DB_HOST=tu-host.supabase.com
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=tu-password
DB_NAME=postgres
DB_SSL=true

# ── JWT ──
JWT_SECRET=pon_un_valor_largo_y_aleatorio_aqui
JWT_EXPIRATION=7d

# ── App ──
PORT=3000
NODE_ENV=production

# ── CORS: URL exacta del frontend en Vercel (sin slash final) ──
FRONTEND_ORIGINS=https://tu-proyecto.vercel.app
